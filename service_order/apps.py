from django.apps import AppConfig


class ServiceOrderConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'service_order'
    verbose_name = 'Controle de Ordem de serviço'
